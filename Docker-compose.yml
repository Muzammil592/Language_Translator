version: '3.8'

services:
  # 1. Backend Service Configuration
  api-server:
    build:
      context: ./Backend    # <-- Isay check karein, capital 'B' hona chahiye
      dockerfile: Dockerfile
    container_name: mern-translator-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    restart: always

# Frontend Service Configuration
  web-client:
    build:
      context: ./Frontend/my-react-app  # <-- Isay check karein, capital 'F' hona chahiye
      dockerfile: Dockerfile
    container_name: mern-translator-frontend
    ports:
      - "80:80"
    restart: always
    depends_on:
      - api-server